<Page x:Class="PolyPaint.Vues.Chat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:chat="clr-namespace:PolyPaint.Controls.Chat"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="White"
             Width="640"
             Title="Chat">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Fill="{StaticResource PrimaryHueLightBrush}"/>
        <chat:Channel Grid.Row="1" Height="60" VerticalAlignment="Top" BorderBrush="{StaticResource PrimaryHueLightBrush}" BorderThickness="2"/>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <Button Width="83" Style="{StaticResource MaterialDesignFlatLightBgButton}"  Foreground="white" materialDesign:ButtonAssist.CornerRadius="0" >
                <materialDesign:PackIcon Kind="Chat" />
            </Button>
            <Button Width="83" Style="{StaticResource MaterialDesignFlatLightBgButton}"  Foreground="white" materialDesign:ButtonAssist.CornerRadius="0" >
                <materialDesign:PackIcon Kind="CardPlus" />
            </Button>
            <Button Width="83" Style="{StaticResource MaterialDesignFlatLightBgButton}" Foreground="white" materialDesign:ButtonAssist.CornerRadius="0" >
                <materialDesign:PackIcon Kind="UserSearchOutline" />
            </Button>
        </StackPanel>

        <Button Content="Disconnect" Margin="0,7,15,0" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Column="1" Command="{Binding DisconnectCommand}"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <chat:MessageList/>
        </Grid>

        <Grid Grid.Row="2" 
              Grid.Column="1"
              MinHeight="60"
              MaxHeight="200">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Grid.ColumnSpan="2" BorderThickness="0 1 0 0" BorderBrush="{StaticResource PrimaryHueLightBrush}"/>
            
            <TextBox Grid.Column="0" Width="auto"
                     Text="{Binding PendingMessage, UpdateSourceTrigger=PropertyChanged}"
                     LostKeyboardFocus="chatBox_LostKeyboardFocus"
                     AcceptsTab="True"
                     x:Name="chatBox"
                     Margin="20,0,0,0"
                     VerticalAlignment="center" />
            <Button Grid.Column="1" Width="100"
                    IsDefault="True"
                    Margin="20,0,15,0"
                    Command="{Binding SendCommand}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Content="Envoyer"/>
        </Grid>

    </Grid>
</Page>
